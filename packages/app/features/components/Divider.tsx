import { View } from 'react-native'

const Divider = () => {
  return (
    <View
      style={{
        borderBottomWidth: 1,
        borderBottomColor: '#eeeeee',
        width: '100%',
      }}
    />
  )
}

export default Divider
